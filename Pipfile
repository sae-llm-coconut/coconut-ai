[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[requires]
python_version = "3.10"

[scripts]
lint-editorconfig = "ec"
lint-autopep8 = "autopep8 --recursive --diff src"
lint-mypy = "mypy src"
start = "python -m example/src"
build = "python setup.py sdist"

[packages]
coconut-ai = {path = ".", editable = true}
